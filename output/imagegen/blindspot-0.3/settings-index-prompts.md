# Blindspot 0.3 — Settings and Index concepts

Generated with the built-in image-generation tool, using a-translucent.png as the approved visual style reference. Preview artwork only; no application code changes. All displayed values are illustrative.

The Index concept proposes eligible-work progress and explicit model telemetry; these are design requirements for future implementation, not a claim that the existing snapshot already supplies them. The web-provider menu is also a proposed presentation of the existing configurable fallback URL; custom URLs must remain supported.

## Settings prompt

Use case: ui-mockup. Create a high-fidelity PNG concept for Blindspot 0.3, a macOS desktop utility. The supplied image is a STYLE REFERENCE: it is the user-approved translucent launcher. Preserve its softly smoked charcoal glass, subtly visible blurred dusk wallpaper, warm white SF Pro-like typography, muted warm amber accent and quiet native macOS feeling. Create a new SETTINGS WINDOW screen, not another search launcher. 1536x1024 landscape canvas, straight-on crisp screen capture style, one centered window filling most of the canvas, roughly 1160x820 px, readable text, no perspective or hardware. Same softly blurred slate/taupe dusk landscape outside the window. One fine outer edge and soft shadow, 18px outer corners. Slightly stronger dark material under the main text area for readable contrast, sidebar a little more translucent. Mac window traffic-light controls in upper left. Two-column layout: 230px sidebar, broad main area. Sidebar has small wordmark "blindspot" near top, then native small subdued line icons with sentence-case labels in this exact order: "General", "Search", "Content", "Index", "Clipboard", "AI", "Appearance", "About". ONE selected sidebar row uses subtle amber-tinted flat highlight. No boxed content cards, no metric tiles, no pills or status badges, no uppercase tracked micro-labels, no decorative sparkle icons. Content lives on one continuous surface; hierarchy comes from typography and generous spacing. At most three subtle horizontal rules, never border every row. Native controls are permitted but do not frame groups in rectangles. Precise aligned left labels and right controls, no marketing copy, no illustrations, no graphs except a meaningful progress track if specified. No text outside the window, no watermark. All counts/content are illustrative mock data, not actual user status; do not add this caveat inside the artwork. SCREEN 1 — GENERAL SETTINGS. Select "General" in sidebar. Main content has title "General" in confident 32px type, subtitle "Shortcuts and everyday behaviour" in readable muted 17px type. Three flat groups separated by whitespace:
Group "Shortcuts". Row "Open Blindspot" with one compact native keyboard shortcut recorder displaying "⌘ Space" right-aligned. Under it, row "Open AI" with shortcut "⌘ ⇧ Space".
Group "Startup". Row "Launch at login", secondary line "Ready whenever you need it.", right-aligned small macOS toggle switched ON in restrained amber.
Group "Search". Row "Results shown", right small native selection control "8" with chevrons. Row "Fallback web search", secondary line "Offered when local results are limited.", right native menu control "DuckDuckGo".
At bottom, one collapsed disclosure row "Advanced settings" with a small chevron. Generous empty space is intentional, not filled with cards. Make the layout look like a beautifully considered native app, not a web dashboard. Keep typography substantially more prominent than outlines.

## Index prompt

Use case: ui-mockup. Create a high-fidelity PNG concept for Blindspot 0.3, a macOS desktop utility. The supplied image is a STYLE REFERENCE: it is the user-approved translucent launcher. Preserve its softly smoked charcoal glass, subtly visible blurred dusk wallpaper, warm white SF Pro-like typography, muted warm amber accent and quiet native macOS feeling. Create a new SETTINGS WINDOW screen, not another search launcher. 1536x1024 landscape canvas, straight-on crisp screen capture style, one centered window filling most of the canvas, roughly 1160x820 px, readable text, no perspective or hardware. Same softly blurred slate/taupe dusk landscape outside the window. One fine outer edge and soft shadow, 18px outer corners. Slightly stronger dark material under the main text area for readable contrast, sidebar a little more translucent. Mac window traffic-light controls in upper left. Two-column layout: 230px sidebar, broad main area. Sidebar has small wordmark "blindspot" near top, then native small subdued line icons with sentence-case labels in this exact order: "General", "Search", "Content", "Index", "Clipboard", "AI", "Appearance", "About". ONE selected sidebar row uses subtle amber-tinted flat highlight. No boxed content cards, no metric tiles, no pills or status badges, no uppercase tracked micro-labels, no decorative sparkle icons. Content lives on one continuous surface; hierarchy comes from typography and generous spacing. At most three subtle horizontal rules, never border every row. Native controls are permitted but do not frame groups in rectangles. Precise aligned left labels and right controls, no marketing copy, no illustrations, no graphs except a meaningful progress track if specified. No text outside the window, no watermark. All counts/content are illustrative mock data, not actual user status; do not add this caveat inside the artwork. SCREEN 2 — INDEX STATUS. Select "Index" in same sidebar. Main content top heading "Index" 32px and a small quiet native text button "Refresh" right aligned. Immediately below, a concise status block without a card: a small amber dot beside title "Building passage embeddings", secondary line "Word search is available while this runs." Then a thin amber progress track and exact caption "2,340 of 3,120 eligible passages · 75%". This illustrates progress for the eligible work queue, NOT total library coverage. Underneath place a single understated summary line "8,426 documents   ·   38,940 passages   ·   812 MB on disk", no separate metric cards or giant KPI numbers.
Then flat section titled "Current work", with simple aligned key-value rows: "Model" — "embeddinggemma:300m"; "Pace" — "Low impact"; "Scope" — "Documents and two other folders".
Next flat section "Needs attention" with tiny muted amber warning icon and the main line "2 documents need a closer look". Below, two compact unboxed rows: "Museum tickets.pdf" with right text "Password protected"; "Field notebook.pdf" with right text "OCR page limit reached". Each row can have small file icon and trailing reveal arrow, no colourful badges.
Finally two collapsed disclosure rows, "Indexed folders" and "Storage and resources", with chevrons. Keep clean hierarchy and enough whitespace, fit without scrolling. Do not add charts, per-folder meters, status tiles, extra buttons or decorative containers. Use the exact text provided and preserve legibility.

