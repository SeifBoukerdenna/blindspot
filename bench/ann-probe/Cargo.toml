[package]
name = "blindspot-ann-probe"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
usearch = { version = "=2.26.2" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
debug = 1
