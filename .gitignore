# Rust build output: the workspace root since the retrieval crate split, and the
# pre-split core/target, which cargo no longer writes to.
/target/
core/target/

# Hand-assembled app bundle
build/

# Machine-specific Claude Code settings (see SETUP.md step 2)
.claude/settings.local.json

.DS_Store
